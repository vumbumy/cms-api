openapi: 3.0.0
info:
  title: Event
  version: "1.0.0"
tags:
  - name: event
    description: 이벤트
  - name: kpi
    description: 데이터 지표 (Event Performance Indicator)
  - name: rule
    description: 데이터 규칙 (Event Schedule)
paths:
  /events:
    post:
      summary: KPI 및 Rule 등록
      tags:
        - kpi
        - rule
      responses:
        '200':
          description: OK
    get:
      summary: Event 및 Record 리스트
      tags:
        - event
      responses:
        '200':
          description: OK
  /kpis:
    get:
      summary: KPI 및 Rul
      tags:
        - kpi
      responses:
        '200':
          description: OK
  /rules:
    get:
      summary: KPI 및 Rul
      tags:
        - rule
      responses:
        '200':
          description: OK